<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      text-align: center;
    }
    input, button {
      padding: 10px;
      margin: 10px 0;
    }
    input {
      width: 80%;
    }
    .result {
      margin-top: 20px;
      padding: 10px;
      background-color: #f0f0f0;
      word-break: break-all;
    }
  </style>
</head>
<body>
  <h1>URL Shortener</h1>
  <form action="/shorten" method="POST">
    <input type="url" name="originalUrl" placeholder="Enter your long URL" required>
    <br>
    <button type="submit">Shorten</button>
  </form>

  <% if (shortUrl) { %>
    <div class="result">
      <p>Short URL: <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a></p>
      <p>Stats: <a href="/stats/<%= shortUrl.split('/').pop() %>">View stats</a></p>
    </div>
  <% } %>
</body>
</html>